# Base RunPod dependency
runpod~=1.7.0

# Core ML Libraries (Update versions based on Flux requirements)
# Check Flux documentation for specific compatible versions!
# These are examples, verify compatibility:
torch>=2.1.0 --extra-index-url https://download.pytorch.org/whl/cu121 # Use CUDA 12.1 if needed by torch/diffusers
transformers>=4.38.0
diffusers>=0.27.0 # Ensure this version supports Flux.1 Schnell
accelerate>=0.27.0
safetensors>=0.4.0

# Optional: For potential speedups
xformers>=0.0.23 # If using enable_xformers_memory_efficient_attention

# R2 Interaction
boto3>=1.28.0
botocore>=1.31.0 # Often updated with boto3

# Image handling and text fixing (often needed by diffusers/transformers)
Pillow>=9.0.0
ftfy>=6.1.0 # Usually a dependency of transformers/clip

# Other common dependencies (keep if needed, remove if not)
scipy>=1.9.0
requests>=2.28.0 # For downloading LoRA

# Remove triton if not explicitly required by Flux or xformers setup
# triton==2.0.0.post1